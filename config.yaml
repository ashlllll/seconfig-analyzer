# ============================================================
# SecConfig Analyzer - Application Configuration
# Student: Qian Zhu (S1034134)
# ============================================================

app:
  name: "SecConfig Analyzer"
  version: "1.0.0"
  debug: false
  log_level: "INFO"

analysis:
  supported_formats:
    - env
    - yaml
    - yml
    - json
  max_file_size_mb: 10
  rules_dir: "data/rules_catalog"
  templates_dir: "data/templates_catalog"

red_team:
  enabled_rules: "all"
  severity_threshold: "low"
  cache_compiled_regex: true

blue_team:
  auto_fix_enabled: true
  backup_original: true
  validation_strict: true

simulation:
  default_iterations: 10000
  confidence_level: 0.95
  seed: 42
  default_distribution: "beta"

llm:
  enabled: false
  provider: "openai"
  model: "gpt-4"
  api_key_env: "OPENAI_API_KEY"
  temperature: 0.8
  max_tokens: 500
  presence_penalty: 0.6
  frequency_penalty: 0.6

ui:
  theme: "light"
  page_title: "SecConfig Analyzer"
  page_icon: "🔒"
  issues_per_page: 10
  chart_height: 400

logging:
  level: "INFO"
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
  file: "logs/app.log"

export:
  json_indent: 2
  csv_delimiter: ","